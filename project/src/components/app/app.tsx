import { Route, BrowserRouter, Routes } from 'react-router-dom';
import React from 'react';

type Props = {
};

const App = ({
}: Props): JSX.Element => (
  <BrowserRouter>
    <Routes>
    </Routes>
  </BrowserRouter>
);

export default App;
